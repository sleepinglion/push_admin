<div class="sl_show">

	<p>
		<b><%=t(:enable,scope:[:activerecord,:attributes,:product]) %></b>
		<%=raw flagToString(@push_prepare_picture.enable) %>
	</p>
	<p>
  	<b><%=t(:label_created_at) %></b>
  	<%= @push_prepare_picture.created_at %>
  </p>
  <p>
  	<b><%=t(:label_updated_at) %></b>
  	<%= @push_prepare_picture.created_at %>
  </p>
</div>
<% if can? :update, @push_prepare_picture %>
<%= link_to t(:link_edit), edit_admin_push_prepare_picture_path(@push_prepare_picture) %> |
<% end %>
<%= link_to t(:link_list), admin_push_prepare_pictures_path %>
